package edu.handong.csee.java.hw3;

import java.lang.reflect.Array;
import java.util.ArrayList;

public class MessageCounter {
	
	ArrayList<String> refinedOne = new ArrayList<String>();
	ArrayList<String> studentNames = new ArrayList<String>();
	
	private int lengthOfArrayList = 0;
	// Need to make an Array of Students' names.
	// instantiate the ArrayList from RawDataRefiner Class.
	// if the name part, let's say
	/*
	for (int i = 0; i<lengthOfArrayList;i++)
	{
		String temp = refinedOne[i]; // One by one, get the String from ArrayList, and count.
		
		for (int j = 0; j< studentNames.size();j++)
		{
		if (temp[9] == studentNames[j]) // let's say that 9th element is where the name starts from
			
		}
	}
	*/
	
	// if the 0th index of the

}
